require 'rspec'
require 'rspec/core/rake_task'

import './db/db.rake'
task default: %w[spec]

task :spec do
  RSpec::Core::RakeTask.new(:spec) do |t|
    t.pattern = Dir.glob('app/spec/**/*_spec.rb')
  end
end
